module.exports = {
  /**
   * Example documentation.
   * @function
   */
  required(value) {
    if (value === null || value === undefined || value === '') {
      throw new Error('Required');
    }
  },

  /**
   * Example documentation.
   * @function
   */
  suspicious(value) {
    if (/[&<>'"/]/.test(value)) {
      throw new Error('Suspicious characters [&<>"\',/]');
    }
  },
};
