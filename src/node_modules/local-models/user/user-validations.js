module.exports = {
  first_name: {
    format: {
      pattern: /[A-Za-z'-.]+/,
      message: 'must contain only letters, \'-.',
    },
  },
  last_name: {
    format: {
      pattern: /[A-Za-z'-.]+/,
      message: 'must contain only letters, \'-.',
    },
  },
  email: { email: true },
  password: {
    format: {
      pattern: /^(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z])(.+)$/,
      message: 'must contain at least 1 uppercase character, 1 lowercase character, and 1 digit',
    },
    length: {
      minimum: 10,
      maximum: 128,
      tooShort: 'must be at least 10 characters',
      tooLong: 'must be less than or equal to 128 characters',
    },
  },
};
