/**
 * Generates a random 8 digit integer
 *
 * @description Used to generate tokens for reset password, locked account, etc.
 * @function
 * @returns {Integer} - An 8 digit integer
 */
module.exports = () => Math.floor(Math.random() * 90000000) + 10000000;
